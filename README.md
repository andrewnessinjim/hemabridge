# Hemabridges

## Running the project

Requires [pnpm](https://pnpm.io) (this repo is pinned to `pnpm@10.29.3` via `packageManager` in `package.json`).

```bash
pnpm install
pnpm dev
```

The dev server runs at [http://localhost:3000](http://localhost:3000).
